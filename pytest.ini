[pytest]
testpaths = tests
addopts = -v --cov=src --cov-report=term-missing --cov-report=html:.coverage_html
python_files = test_*.py
asyncio_mode = auto
markers =
    slow: tests that score a large sample or run property-based sweeps (deselect with -m "not slow")
