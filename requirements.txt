# Runtime
pydantic==2.13.4
pydantic-settings==2.15.0
xgboost==3.4.0
numpy>=1.26,<3
joblib>=1.3,<2
aiokafka==0.11.0   # issue-008: platform-wide standard, replaces kafka-python. Used by our local
                    # shared/kafka/ stand-in -- see that package's docstring. Swap out if/when the
                    # real shared.kafka package is provided.
orjson==3.10.15    # AD-032: mandated for Kafka serialization
prometheus-client==0.21.1
fastapi==0.115.6
uvicorn[standard]==0.34.0

# Training only (not needed in the runtime image)
scikit-learn==1.8.0
lightgbm==4.7.0
shap==0.52.0
matplotlib>=3.8
seaborn>=0.13
mlflow==3.15.1
jupyter
nbformat
nbconvert
pandas>=2.2

# Tests only
pytest>=8.0,<10
pytest-cov>=5.0
pytest-asyncio>=0.24  # added 2026-08-20 for the async CorrelationMLConsumer tests (pytest.ini: asyncio_mode = auto)
hypothesis>=6.100
httpx>=0.27  # FastAPI TestClient dependency; also used directly by stress/run_load_test.py
