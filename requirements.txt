# Runtime
pydantic==2.13.4
pydantic-settings==2.15.0
xgboost==3.4.0
numpy>=1.26,<3
joblib>=1.3,<2
confluent-kafka==2.6.1   # used by StandaloneRunner when shared.kafka isn't available -- drop if the
                          # real shared.kafka package brings its own client (e.g. aiokafka)

# Training only (not needed in the runtime image)
scikit-learn==1.8.0
lightgbm==4.7.0
shap==0.52.0
matplotlib>=3.8
seaborn>=0.13
mlflow==3.15.1
jupyter
nbformat
nbconvert
pandas>=2.2

# Tests only
pytest>=8.0,<10
pytest-cov>=5.0
hypothesis>=6.100
